const flightUrl = '/flight'
const accommodationUrl = '/accommodation'
const userUrl = '/user'
const bookingUrl = '/booking'
const tourUrl = '/tour'

export const routePaths = {
    // flight
    flights: flightUrl,
    // accommodation
    accommodation: accommodationUrl,
    // booking
    booking: bookingUrl,
    bookingCreateSuccess: `${bookingUrl}/create-success`,
    // user
    user: userUrl,
}
