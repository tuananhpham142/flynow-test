import { CheckboxProps } from '../Checkbox/Checkbox.types';
import { RHFBaseProps } from '../theme/types.types';

export interface RHFCheckboxProps extends RHFBaseProps {
    checkboxProps: CheckboxProps;
}
