'use client';
import NotFoundPage from '@acme/pages/components/not-found';

type Props = {};

const NotFound = (props: Props) => {
    return <NotFoundPage />;
};

export default NotFound;

// rebase dev
